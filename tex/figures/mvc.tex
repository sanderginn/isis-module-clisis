\begin{tikzpicture}[
    node/.style={
      draw=maincolor,
      thick,
      rectangle,
      rounded corners,
      minimum width=6em,
      minimum height=7ex,
      inner sep=0ex,
      fill=maincolor!18},
    shorten >=1pt,
    xs/.style={every edge/.append style={
      transform canvas={xshift=#1}
    }},
    ys/.style={every edge/.append style={
      transform canvas={yshift=#1}
    }},
    line/.style={
      thick,
      color=maincolor
    },
    node distance=4em and 4em
  ]

  \node[node] (Controller) {Controller};
  \node[node, below left=of Controller] (View) {View};
  \node[node, below right=of Controller] (Model) {Model};


  \draw[->, rounded corners=5pt]
    (Controller) -| node[fill=white, near end]{update} (View);
    
  \draw[->, rounded corners=5pt]
    (Controller) -| node[fill=white, near end]{update} (Model);
    
  \draw[->, rounded corners=5pt]
    (View.north east) -| node[fill=white, near end]{user action} (Controller.south);
    
  \draw[->,]
  	(View.south east) -- node[fill=white]{get changed state} (Model.south west);
  
  \draw[->, rounded corners=5pt]
    (Model) -- node[fill=white, yshift=-0.5pt]{notify} (View.east);

\end{tikzpicture}