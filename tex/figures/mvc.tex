\begin{tikzpicture}[
    node/.style={
      draw=maincolor,
      thick,
      rectangle,
      rounded corners,
      minimum width=6em,
      minimum height=7ex,
      inner sep=0ex,
      fill=maincolor!18},
    shorten >=1pt,
    xs/.style={every edge/.append style={
      transform canvas={xshift=#1}
    }},
    ys/.style={every edge/.append style={
      transform canvas={yshift=#1}
    }},
    line/.style={
      thick,
      color=maincolor
    },
    node distance=4em and 4em
  ]

  \node[node] (Controller) {Controller};
  \node[node, below left=of Controller] (View) {View};
  \node[node, below right=of Controller] (Model) {Model};


  \draw[->, rounded corners=5pt]
    (Controller) -| node[fill=white, near end]{update} (View);
    
  \draw[->, rounded corners=5pt]
    (Controller) -| node[fill=white, near end]{update} (Model);
    
  \draw[->, rounded corners=5pt]
    (View) -| node[fill=white, near end, left, xshift=10pt]{user action} ($(Controller.south west)!0.5!(Controller.south)$);
  
  \draw[->, rounded corners=5pt]
    (Model) -| node[fill=white, near end, right, xshift=-10pt, yshift=-0.5pt]{notify} ($(Controller.south east)!0.5!(Controller.south)$);

\end{tikzpicture}